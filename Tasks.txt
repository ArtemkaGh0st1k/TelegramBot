Необходимо!!!

Примечание: 
конструкция async with sqlite3.connetcion() as db:
не работает т.к данный модуль не поддерживает 
ассинхроный вызов!

1) Продумать архитектуру кода: как расположить
модули по пакетам

    1.1) Константы пишутся с большой буквы т.е CapsLock:
    в MainParamaeters.py переминовать такие переменные(ok)

    1.2) Чтобы привильно спрятать свой токен:
    есть в уроке номер 3 у Python Hub Studio 

    1.3) Чтобы расположить разные хендлеры по папкам
    нужно подключить класс Router() к скрипту (ok)

    1.4) Создать словарь типов комманд (ok)

    1.5) Указать явный тип исключений в блоках 
    try/catch


2) Как привильно расписать Main.py модуль, где
будут происходить вызов функция из разных модулей(ok)

3) Зафиксировать в словарь RegisterDispatcherDictionary (не нужно было)

4) Продумать call_back: также возможно проиграть 
с привязкой к собитиям

5) Продумать момент, когда будет возникать ошибка в 
потоке и что будет делать бот: останавливаться или 
присылать уведомление

6) Использование logging
Возможно как отдельынй декоратор
или внутри функции

    6.1) В файле .log было бы интересно
    когда всплывает ошибка, он по гиперсылке 
    переходил в это место

    6.2) использовать модуль logging 
    по полному в проекте

7) Нужно ли использовать __init__ в пакете при создании
базы данных?

8) Разница м/у выбором кнопок: InlineKeyboard или 
ReplyKeyboardButton -> call_back

9) Применить несколько таблиц для расширении базы данных

10) Каждый раз удалять клавиатуру ReplyKeyboardRemove(),
чтобы она не сохранялась при заходе в телеграм

11) Добавить ко всем раскладкам кнопку НАЗАД

12) Добавление проекта в GitHub:

    12.1) Указать версии библиотек и модулей
    в виде .txt файле  

    12.2) Сохранить токен в .env файле

    12.3) gitignore, пошаманить  
